# setup.py

from setuptools import setup, find_packages

setup(
    name='chaoticpy-ai',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # List your dependencies here
    ],
)
